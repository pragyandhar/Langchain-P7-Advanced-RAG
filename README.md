# Langchain-P7-Advanced-RAG
The core problem with basic RAG is that retrieving the top k chunks by similarity score alone is naive — you often get redundant, irrelevant, or poorly ordered results. Project 7 introduces two techniques that fix this: re-ranking and advanced retrieval strategies.
